{% extends 'base.html.twig' %}

{% block title %}Spent{% endblock %}

{% block body %}
    <h1>Spent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spent.id }}</td>
            </tr>
            <tr>
                <th>Expense_name</th>
                <td>{{ spent.expenseName }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ spent.cost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('spent_index') }}">back to list</a>

    <a href="{{ path('spent_edit', {'id': spent.id}) }}">edit</a>

    {{ include('spent/_delete_form.html.twig') }}
{% endblock %}
